/* 3.0+1 will be..? a.Integer b.FloatingPointNumber c.Character
Ans - b.FloatingPointNumber */

#include <stdio.h>

int main()
{
    int a = 1;
    float b = 3.0;
    printf("Sum of 3.0 and 1 will be %f\n", a + b);
    printf("Sum of 3.0 and 1 will be %d\n", a + b);

    return 0;
}