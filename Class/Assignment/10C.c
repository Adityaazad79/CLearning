/* A text may be comprised of multiple lines. Teacher has given two words(first word, second word) arbitrarily. The assignment is if the first word is present (may be multiple times) in the text, then change the first word by second word wherever the first word occurs and print the text. But if the first word has no existence in the text then print -1. 

Input:

First line provides the input text followed by 

the second line contains the first word followed by 

the third line contains the second word.

Output:

The modified text will be printed if the first word is present in the text otherwise print -1. */

#include <stdio.h> 

int main()
{
    

    return 0;
}